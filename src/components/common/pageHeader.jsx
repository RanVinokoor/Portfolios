const PageHeader = ({ title }) => {
  return (
    <div className="row">
      <div className="col-12">
        <h1 className="title">{title}</h1>
      </div>
    </div>
  );
};

export default PageHeader;
